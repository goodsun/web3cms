{
  "name": "fullstack-serverless-cdk",
  "version": "1.0.0",
  "description": "Fullstack serverless application using AWS CDK",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "build:frontend": "cd frontend && npm run build && cp -r js css dist/",
    "deploy:dev": "npm run build:frontend && cdk deploy --context env=dev && ./scripts/update-frontend-config.sh dev",
    "deploy:staging": "npm run build:frontend && cdk deploy --context env=staging && ./scripts/update-frontend-config.sh staging",
    "deploy:prod": "npm run build:frontend && cdk deploy --context env=prod && ./scripts/update-frontend-config.sh prod",
    "deploy:dev:base": "cdk deploy --context env=dev",
    "deploy:staging:base": "cdk deploy --context env=staging",
    "deploy:prod:base": "cdk deploy --context env=prod",
    "update-config:dev": "./scripts/update-frontend-config.sh dev",
    "update-config:staging": "./scripts/update-frontend-config.sh staging",
    "update-config:prod": "./scripts/update-frontend-config.sh prod",
    "destroy": "cdk destroy",
    "destroy:dev": "cdk destroy --context env=dev",
    "destroy:staging": "cdk destroy --context env=staging",
    "destroy:prod": "cdk destroy --context env=prod",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "dev:api": "sam local start-api --template cdk.out/*.template.json --env-vars env.json",
    "dev:frontend": "cd frontend && npx live-server --port=8080",
    "dev:db": "docker run -d -p 8000:8000 amazon/dynamodb-local",
    "dev": "echo 'Starting local development...' && npm run build && npm run synth && concurrently \"npm run dev:api\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/jest": "^29.5.5",
    "@types/node": "20.8.4",
    "aws-cdk": "^2.150.0",
    "aws-cdk-lib": "^2.150.0",
    "concurrently": "^9.2.0",
    "constructs": "^10.3.0",
    "esbuild": "^0.19.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.445.0",
    "@aws-sdk/lib-dynamodb": "^3.445.0",
    "dotenv": "^17.2.1",
    "source-map-support": "^0.5.21"
  }
}
